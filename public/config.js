/**
 * StockPulse — Configuration
 *
 * After deploying the Cloudflare Worker, paste its URL below.
 * Format: https://stock-pulse-proxy.YOUR-SUBDOMAIN.workers.dev
 *
 * Leave as empty string to see a helpful setup message in the app.
 */
const CONFIG = {
  WORKER_URL: '', // ← paste your Worker URL here after: wrangler deploy
};
